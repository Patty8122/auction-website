{
  "name": "auction-service",
  "version": "1.0.0",
  "description": "auction service",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js",
    "migrate:up": "node-pg-migrate up -m ./migrations --env dev",
    "migrate:down": "node-pg-migrate down -m ./migrations --env dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-pg-migrate": "^6.2.2",
    "pg": "^8.11.3"
  }
}
